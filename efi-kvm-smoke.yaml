name: efi-kvm-smoke
cpu:
    cores: 1
memory: 512M
console: stdio
bootrom: ./firmware/SCORPI_EFI.fd

devices:
  pci:
    - device: hostbridge
      slot: 0

    - device: virtio-blk
      slot: 2
      path: ./FreeBSD-14.2-RELEASE-arm64-aarch64-bootonly.iso
      ro: true
